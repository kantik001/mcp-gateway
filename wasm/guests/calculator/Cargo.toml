[package]
name = "calculator"
version = "0.1.0"
edition = "2021"
description = "Sandboxed calculator guest for mcp-gateway (C ABI exports, no FS/net)"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
